

x = [0 0.2 0.4 0.6 0.8 1];

p_0 = pi * cos(pi * x(1));
p_l = pi * cos(pi * x(6));

f = sin(pi*x);
alpha = komplint(5,x,p,p_0, p_l);

xp = l

n = 6; %trebamo i nacrtati, treba nam prosirena particija

%nacrtat cemo 101 duzinu