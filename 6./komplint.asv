function a = deBoorCox(l, x, p, p_0, p_l)
%provjeriti jel p dimenzije l+1
    
 
     t = [x(1) x(1) x(1) x x(size(x,2)) x(size(x,2)) x(size(x,2))];

     i = bintrazenje(x,t);

     B = zeros(n,n);
     B(1,1) = 1;
     B(n,n) = n;

     for j = 2 : n 
         if(j == 2)
         if()
         
     end

   
    p = [p(1) p_0 p(2:(l)) p_l p(l+1)];

    a = B \ p; 
end